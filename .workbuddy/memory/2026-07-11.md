# 2026-07-11

## 架构澄清：LLM 接入方式 + ad-agent/ua-monitor skill 复用性
- 用户问：智能体控制台是接 LLM API 还是嵌 SDK？旧 skill 能否直接用？
- **LLM 接入方式（代码实证）**：
  - 设计上走**自研多模型路由层**（`services/llm/router.py`：Claude/GPT4/DeepSeek/Local 四类 Provider + 能力评分 + 优雅降级），本质是对接各家 **chat-completion HTTP API**（OpenAI-compatible / Anthropic），**未嵌入任何厂商 Agent SDK**（非 OpenAI Assistants/Agents SDK、非 LangChain）。
  - 关键事实：各 Provider 的 `chat_completion()` 目前是 **placeholder**（返回 "Claude response placeholder" 等，注释"实际实现需要导入 openai/anthropic SDK"）。`loop.py:196` 调 `get_llm_router()` 若未初始化 config 则返 None → `is_llm_available()`=False → `_decide()` 落到 `_rule_based_decide()`。
  - 即**当前默认运行 = 纯规则引擎**，LLM 大脑是桩；要真接大模型只需：①实现某 Provider 的 `chat_completion`（装 openai/anthropic SDK 或 httpx 调 API）；②启动时用 config.py 里的 `openai_api_key/base_url/model`、`claude_base_url` 调 `get_llm_router(config)` 初始化实例。
- **ad-agent / ua-monitor skill 复用性（代码实证）**：
  - 两者均存在于 `~/.workbuddy/skills/`，但 frontmatter 均 `disable: true` → 当前**不可作为 Skill 直接加载使用**。
  - 形态：ad-agent=NL→Schema→API 的意图驱动投放助手（依赖 ua-monitor，含 meta/google/tiktok/af/dsp adapters + schemas/v* + action_log/effect_trk，哲学"LLM 只做 NL→Schema，Schema→API 确定性映射零幻觉"）；ua-monitor=MI API→SQLite 的 UA 分析与盯盘干预引擎（含 mi_fetch.py、17 个 SQL 模板、分析报告模板，隐私优先）。
  - SmartUA 是这俩 skill 的**架构提炼产物**（非代码 import）：Tool Registry/TOOL_TO_ACTION + L0-L3 分级 ↔ ad-agent 的 tiered action controls；episode memory/impact_* ↔ ad-agent learner/effect_trk；autonomy detector ↔ ua-monitor 盯盘干预引擎+SQL 模板。
  - 结论：**不能直接"装进"SmartUA**；但**可移植其资产**——ua-monitor 的 `sql-templates.md`/`mi_fetch.py` 可支撑真实数仓适配器（接 MI/Meta 需账户，Meta 仍被封），ad-agent 的 `schemas/*` 意图→动作 JSON 可直接对齐 Tool Registry。

## 接入火山引擎方舟（Volcengine Ark）作为国内 LLM（08:47）
- 用户问能否用国内模型（火山方舟），要求加配置（key 稍后发）。已完成接入（OpenAI 兼容协议，零新依赖，复用已装的 httpx 0.28.1）。
- 改动：
  - `config.py`：新增 `ark_api_key/ark_model/ark_base_url`（默认 `https://ark.cn-beijing.volces.com/api/v3`）；`get_llm_providers_config()` 加 `"ark"` provider（capabilities 含 complex_intent/strategy_analysis/creative_generation/fast_response，priority=1）与两条路由规则（`campaign.optimize_batch`、`creative.rotate` 首选 ark，分别降级 claude/gpt4）。
  - `router.py`：新增 `ArkProvider`（基于 httpx.AsyncClient 调 `{base_url}/chat/completions`，Bearer 鉴权，解析 `choices[0].message.content`），并在 `provider_classes` 注册 `"ark": ArkProvider`；顶部加 `import httpx`。
  - `main.py`：lifespan 启动期调用 `get_llm_router(settings.get_llm_providers_config())` 初始化——修复此前 router 仅在 llm.py 端点命中时才初始化的漏洞，使 Agent Loop 的 `_llm_decide` 真正能用上大模型（否则永远规则兜底）。
  - 新建 `backend/.env`：`ARK_API_KEY=`、`ARK_MODEL=ep-xxxxxxxx`、`ARK_BASE_URL=...`，含使用说明；pydantic-settings 自动加载。
- 验证：providers 含 ark；无 key 时不可用；填假 key 后 `route()` 正确选 ark；`ArkProvider.chat_completion` 确为真实调用（非 placeholder）；`import main` 通过。
- 关键提醒（待用户发 key 时告知）：Ark 的 `model` 参数必须填**推理接入点 Endpoint ID（ep-xxxx）**，不是模型名；用户需在方舟控制台创建接入点 + API 密钥，把两者填进 `backend/.env` 的 `ARK_MODEL` / `ARK_API_KEY`。

## 用户已发 Ark API Key（08:50）
- 用户提供了 `ARK_API_KEY=ark-f2189af4-f4b6-4db7-ab1a-dac78d6d9a19-2b98b`，已写入 `backend/.env`（Edit 替换占位空值）。
- 离线冒烟验证（system python3.14 + 项目依赖）：`settings.ark_api_key` 加载成功=True；`route('creative.rotate')`→ark、`route('campaign.optimize_batch')`→ark 均正确选中 Ark provider（说明 key 存在时被判定为可用）。
- **仍缺**：`ARK_MODEL` 还是占位符 `ep-xxxxxxxx`。真实调用 Ark 必须填 Endpoint ID（在方舟控制台「在线推理」创建接入点得到，形如 `ep-xxxx`）。已再次请用户补此项；填好后即可做真实 API 连通性验证（call ArkProvider.chat_completion 一个测试 prompt）。

## 用户又提供 Ark API URL（08:52）
- 用户给 `https://ark.cn-beijing.volces.com/api/coding/v3`。实测：该路径是 Ark **「代码规划(coding plan)」专用端点**，只接受支持 coding 特性的代码模型；通用 Chat Completions 应走 `https://ark.cn-beijing.volces.com/api/v3`。
- curl 实测结论（用真实 key）：
  - `/api/coding/v3/chat/completions` + 真实 key → 401(初因我 curl 多打 XrL7)→修正后 **404 UnsupportedModel: does not support the coding plan feature**（证实路径存在但非通用）。
  - `/api/v3/chat/completions` + 真实 key + 占位模型 ep-xxxxxxxx → **404 InvalidEndpointOrModel.NotFound**（证实 base 正确、key 鉴权通过、已抵达 Chat 服务，仅模型不存在）。
- 处理：把 `.env` 的 `ARK_BASE_URL` 改回标准 `https://ark.cn-beijing.volces.com/api/v3`（已写回）。**关键结论：Ark 通用聊天用 `/api/v3`，`/coding/v3` 是代码模型专用，SmartUA 不能用。**
- 仍待用户：提供真实 Endpoint ID（ep-xxxx）填进 `ARK_MODEL`。填好即可用 ArkProvider 做端到端真实调用验证。

## backend/.env 加入 gitignore（08:53）
- 用户要求把 `.env` 加进 gitignore。实测根 `.gitignore` 已有 `*.env` 规则，且 `backend/.env` **从未被跟踪**（git status 不显示），密钥本就受保护。
- 为明确防误提交，在 `.gitignore` 的 Environment 段补显式一行 `backend/.env`（注释说明含方舟 Key）。`git check-ignore -v` 确认命中 line 29，`git status` 不再列出该文件。
- 注：若日后需团队共享配置，建议另建 `backend/.env.example`（无密钥的占位模板）提交入库。

## Ark 端到端真实调用验证通过（08:57）
- 用户给 `ARK_MODEL=ep-20260711085626-cmh69`，写入 `.env`；三项齐备：key 有效 + base=`/api/v3` 标准通用端点 + model=真实 ep-xxxx。
- curl 验证：`POST /api/v3/chat/completions` 返回 **HTTP 200 + 真实回答**（ROI 定义），证明端点/key/model 全通。
- **沙箱网络关键事实**：本机出网必须经本地代理 `HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:58910`（curl 默认走、直连超时）。`.env` 由 pydantic 加载，普通 `os.getenv` 读不到，须用 `settings.ark_api_key` 取值。
- **修复 ArkProvider 隐患**：原 `httpx.AsyncClient(timeout=60.0)` 超时太短 + 未显式走代理 → 推理模型长思考触发 `ReadTimeout`。已改 `router.py`：加 `import os`；client 用 `proxy=os.getenv('HTTPS_PROXY') or HTTP_PROXY`、`timeout=httpx.Timeout(120.0, connect=15.0)`、`trust_env=True`。
- 修复后 `ArkProvider.chat_completion` 端到端成功：返回真实广告语，`usage.reasoning_tokens=4243` 证实是推理模型。
- **可复用经验（外部 API 调用通用）**：在此沙箱用 httpx 调任何外网 API，必须显式带本地代理且为推理/慢模型放宽超时；否则直连/短超时必失败。建议在跨项目记忆中固化此网络约束。

## 本地启动前后端走测（09:07）
- 用户确认代理已开，启动后端+前端供走测。
- 后端：`cd backend && /Library/Frameworks/Python.framework/Versions/3.14/bin/python3 -m uvicorn main:app --host 0.0.0.0 --port 8000`（系统 py3.14 带依赖；managed py3.13 缺 pydantic_settings）。启动干净：APScheduler 调度起、主动自治巡检运行、无报错。lifespan 内已初始化 LLM router（含 ark）。
- 前端：`cd frontend && /Users/hezan/.workbuddy/binaries/node/versions/22.22.2/bin/npm run dev`。**Vite 绑在 IPv6 `localhost` → 实际地址 http://localhost:3000（不是 5173，被别的项目占用）**。
- **端口/IPv6 注意**：curl 用 `127.0.0.1:3000` 连不上（vite 仅监听 ::1），须用 `http://localhost:3000`；且本沙箱 curl 默认走 `HTTP_PROXY=127.0.0.1:58910`，访问 localhost 会 502，需 `--noproxy '*'`（8000 因绑 0.0.0.0 直连可用）。浏览器访问 localhost:3000 正常。
- 验证：前端 `/api` 代理转发后端正常（404=路径不对但链路通）；登录 `POST /api/v1/auth/login` {email:admin@smartua.com,password:admin123} → 200 + token（DB 种子账号可用）。
- 待用户浏览器走测智能体控制台（Ark 现已真实接入）。

## 修复「agent 启动失败」（09:10）
- 现象：用户走测点「启动智能体」前端报"启动失败"。
- **根因**：`POST /agent/sessions` 同步跑完整 Agent Loop（现已真调方舟推理模型，单次决策数十秒、整轮数分钟），而前端 `api.js` 的 axios `timeout:10000`（10s）+ Vite 代理 120s 超时 → 浏览器先超时、`e.response` 为空 → 显示"启动失败"。后端日志其实方舟已 200（链路没坏，只是慢）。
- **修法（正确的异步形态）**：
  - 后端 `api/v1/agent.py`：新增 `_spawn_loop(method, session, user, app_id, **kw)`——用 `threading.Thread(daemon=True)` 后台跑 `AgentLoop.start/approve/send_message`，从 `SessionLocal()` 取独立 DB 会话（SQLite 已 `check_same_thread=False`，线程安全）；主请求**立即返回会话**（`status=running`，会话默认即 running，正好驱动前端轮询）；异常落到 `session.status="failed"` + 追加 FINAL 错误步骤。三个接口 `create_session`/`approve_step`/`send_message` 改为 `await`→后台 + 立即 return；`approve/message` 返回前先把 `session.status="running"` 让前端继续轮询。
  - 前端 `AgentConsole.jsx`：加 `sessionTimer=useRef`；新增 effect：当 `session?.status==='running'` 时每 2.5s 调 `agentAPI.getSession(session.id)` 实时拉步骤（deps `[session?.status, session?.id]`）。
- 验证：重启后端（uvicorn 无 `--reload`，必须重启）；`curl POST /agent/sessions` **0.004s 立即返回 status=running**；轮询该会话 → `status=done steps=5 last_kind=final`，末步是方舟真实分析文本（"app 1账户现状分析结果如下…"），**端到端真调方舟跑通**。前端 `npm run build` 通过（3653 模块，无编译错误）。
- **注意**：改了该 `agent.py`/`AgentConsole.jsx` 后，uvicorn（无 reload）与前端 HMR 都需重载；走测前务必重启后端。本次重启后端(PID 新)、前端(:3000)均干净，已验证 200。

## 智能体控制台改造为 SSE 实时流式（09:30~）
- 用户要求：响应时间长，把请求/返回的明细信息 stream 出来（边跑边显示）。
- **方案（零侵入核心模型/Loop）**：后端新增 **SSE 端点** `+` 前端用 **EventSource** 替换 2.5s 轮询。
  - 后端 `api/v1/agent.py`：新增 `_sse(event,data)` 序列化、`_authenticate(token,authorization,db)`（token 走 query 兼容 EventSource 无法自定义 Header，或 Authorization Header）、`GET /agent/sessions/{id}/stream`（StreamingResponse text/event-stream）。
    - 逻辑：连接即 yield `snapshot`(已有 steps+status) + `status`；之后每 0.3s 轮询 `store.get(id)`，新 step 逐条 yield `step`、状态变化 yield `status`、终态 `done/failed` yield `end` 后 break；安全上限 ~30min 防连接泄漏。新增 import：`asyncio, json, Header, Query, StreamingResponse`。
  - 前端 `AgentConsole.jsx`：把 `sessionTimer` ref 改为 `esRef`；新增 effect：当 `session?.id` 存在时 `new EventSource('/api/v1/agent/sessions/{id}/stream?token='+localStorage token)`，监听 `snapshot`(merge steps)/`step`(append)/`status`/`end`(关流)；终态或切换会话时关闭。**running 时卡片标题显示"实时流式"Tag**。
  - `vite.config.js`：代理 `/api` 加 `configure` 钩子，对 `proxyRes` 设 `cache-control: no-cache, no-transform` + `x-accel-buffering: no`，**禁用代理缓冲**保证 SSE 逐条下发。
- **关键网络事实更新**：本沙箱代理地址**已变**为 `http://127.0.0.1:7890`（此前记的 58910 失效）。curl 访问 localhost/127.0.0.1 须 `--noproxy '*'`；后端 ArkProvider 经 env 的 `HTTPS_PROXY=7890` 调通方舟。已同步修正跨项目记忆 `~/.workbuddy/MEMORY.md`。

## 机器意外重启 + 恢复（09:52）
- 用户报告断电机器重启。确认：磁盘代码完整无损（SSE 改造均在磁盘，py_compile 通过）；内存进程全清（端口 8000/3000 free、无 uvicorn/vite 进程）。
- 已重启前后端服务（命令同 09:07 段），均 200 健康。
- **SSE 流式二次验证通过**：建会话立即返回 running → 订阅 `/stream` 端点，连接瞬间收到 snapshot+前 2 步（@0s），81s 后又增量收到 2 步（@81s）→ 确认"边跑边显示"生效，Loop 后台真调方舟推进（推理模型单步 ~40s+ 属正常）。

## 修复「看不到 agent 思考和判断过程」（10:10~）
- 用户走测反馈：SSE 通道通了，但**没看到 agent 在思考和判断的过程**。根因：方舟是推理模型，真正的思考在响应的 `reasoning_content` 字段（之前验证 `reasoning_tokens=4243` 即 4000+ token 思考被 `ArkProvider` 直接丢弃，只抓了 `content`）；Loop 的 `thought` 步只有一句薄摘要，无判断链路。
- **修法（让思考实时 stream 出来）**：
  - `session.py`：`AgentStepKind` 新增 `REASONING="reasoning"`（大模型思考过程）；`AgentStepStatus` 新增 `THINKING="thinking"`；`short()` tag 补 🧠。
  - `router.py` `ArkProvider.chat_completion`：非流式路径捕获 `msg.reasoning_content` 进返回 dict 的 `reasoning`；新增 `stream=True` 支持 —— `client.stream(POST .../chat/completions, json={...stream:true})`，逐行解析 OpenAI SSE，`yield {"type":"reasoning"/"content","text": delta}`，超时放宽到 180s。`LLMRouter.chat_completion` 透传 `stream`：主 provider 尝试流式（仅 Ark 支持），失败则非流兜底。
  - `loop.py`：`Decision` 加 `reasoning` 字段；`_llm_decide` 在调用前先 `session.add_step(REASONING, "🤔 正在调用大模型进行推理…", status=thinking)` 占位，随后 `router.chat_completion(..., stream=True)`，流式把 `reasoning_content` 累积进该 step.text（实时），结束后定稿 `status=done`（无 reasoning 则填"（模型未返回显式思考过程）"）；异常时定稿并退回规则引擎。`_dispatch` 仍保留 `THOUGHT(decision.thought)` 作"决策理由"。
  - `agent.py` `stream_session` 的 `event_gen`：新增 `step_sig` 跟踪每 step 的 (文本长度, status)，**已有步骤文本/状态变化则重发该 step 事件**（原先只发新增步骤），使思考文本增长能实时推到前端。
  - 前端 `AgentConsole.jsx`：`KIND_META` 加 `reasoning`；`StepView` 加 `reasoning` 分支（紫色卡片 `#f9f0ff`、可滚动 `maxHeight:300` `white-space:pre-wrap`、思考中显示 `Spin`）；SSE `step` 事件由"去重跳过"改为**按 id 原地合并更新**，支持思考文本实时增长。
- **验证通过**：订阅 SSE，会话创建瞬间（@0s）即出现 `🧠 思考中…`(thinking)，思考文本**逐秒增长** 14→65→184→297→388→（首轮决策，380字定稿）→ 第二轮 34→185→305→434→426字 → end(done)。确认方舟思考过程已真实流式呈现（两轮 LLM 决策各一段）。前端 `npm run build` 未跑，但 py_compile 4 文件全过、逻辑直白。

## 版本发布：v1.7.0 + CHANGELOG（11:17）
- 用户要求：保存版本 v1.7（ark推理服务对接&流式展示&外部检索），并把 v1.6.0→当前的主要功能更新整理到 changelog。
- 改动：`backend/main.py:49` 版本 `1.6.0` → `1.7.0`。
- 新建 `CHANGELOG.md`（项目根，中文，Keep a Changelog 风格）：
  - v1.7.0（2026-07-11）：Ark 推理对接（config/router/main 三处 + 端到端验证 reasoning_tokens=4243）+ SSE 流式展示（stream 端点/后台线程/REASONING 步/禁缓冲）+ 外部检索（market_research 工具 + BENCHMARK_DB 兜底 + prompt 强约束）+ 可打断&中途改向（abort/redirect）。
  - v1.6.0（2026-07-10，baseline）：主动自治调度 + 种子数据。
- 未自动建 git tag（避免误标 commit）；已向用户说明可补 `git tag v1.7.0` 若需要。

## 关闭前后端服务（11:27）
- 用户要求关闭服务。经 lsof 按端口定位并终止：
  - 后端 uvicorn (PID 6944, :8000) SIGTERM 已停。
  - 前端 vite (PID 6591, :3000 IPv6 [::1]) 首次 SIGTERM 未生效，改 SIGKILL(-9) + `pkill -9 -f "node.*vite"` 成功。
  - 注：lsof 中的 PID 830 是 WorkBuddy 桌面端到 vite 的连接（非服务进程），未杀。
- 验证：`curl --noproxy '*' :8000/health` 与 `:3000` 均 000；lsof 三端口无 listener。**服务已全停，下次走测需重新启动。**

## changelog 归位（11:32）【此条判断有误，见下方修正】
- 曾误判 `docs/changelog.md` 原本不存在，直接 `mv CHANGELOG.md docs/changelog.md`（实际覆盖了原有详细历史）。

## 修正：changelog 合并事故（11:34）
- 错误根因：macOS 文件系统大小写不敏感，`docs/CHANGELOG.md`（git 跟踪，大写）与 `docs/changelog.md` 是同一文件；`mv` 静默覆盖；且 glob `CHANGELOG*` 不匹配小写 `changelog.md` 致误判"不存在"。
- 恢复：从 `git show HEAD:docs/CHANGELOG.md` 取回原 351 行历史（v1.0~v1.6 + 路线图），用脚本把 v1.7.0 段插到顶部、原内容原样保留，并在路线图补 v1.7.0 已完成条目。
- 验证：从 `## v1.6.0 (Phase 4)` 到 `v0.1.0`、路线图均与原文件逐字一致，仅末尾多了 v1.7.0 roadmap 行。**已完成"合并而非替换"**。教训已记入 `~/.workbuddy/MEMORY.md`（文件系统大小写 + mv 覆盖陷阱）。

## 下一阶段迭代设计（21:07）
- 用户问：三大迭代重点（①工具/MCP/CLI/API 供给让 agent 更聪明 ②复盘学策略 ③投知识库）是否可行、能否展开设计。
- 产出：`docs/AGENT_NEXT_ITERATION_DESIGN.md`（设计稿）+ 三层闭环架构图。
- 核心论点：三件事是同一"感知-决策-记忆-学习"闭环的三层扩张——供给层(工具/MCP/CLI/API→ToolRegistry)、认知层(AgentLoop↔Reflector↔StrategyStore↔Planner)、知识层(KB:媒体变更/行业基线/策略效果,SQLite+向量)。
- 所有新能力都建立在既有零件（ToolRegistry/AgentLoop/EpisodicMemory/Reflector/StrategyStore/仿真引擎/market_research/MCP框架）之上，增量可验证。
- 分阶段：Phase4 工具供给(ToolCatalog+MCP桥+CLI沙箱+API自动生成) → Phase5 深度策略学习(StrategyStore扩维+贝叶斯/回归+反事实仿真+策略治理UI+夜间批学习) → Phase6 知识库(SQLite+向量+摄取管线+kb_query+主动学习) → Phase7 集成提自治。
- 设计稿末尾列了 4 个待确认决策点（Phase 优先级/MCP 优先类/向量检索自建还是接服务/策略人审强度）。**已交付，等用户评审反馈。**

## Phase A 真实数据地基 实施完成（v1.8.0, 2026-07-11）
- 用户此前确认首迭代 = **Phase A 地基优先**（A1 持久化 + A2 真实渠道 + A3 真实归因）；真实渠道策略 = 写代码+Mock 待命；Phase D 仅列路线不实现。本轮完成 Phase A 三子任务并通过冒烟（接续前序未收尾的文档/记忆）。
- **A1 状态持久化（双轨存储 = 内存缓存 + SQLite）**：
  - `db/base.py` 启 SQLite **WAL + busy_timeout=5000**，使后台 Agent 线程与请求事务可并发落库（消除 checkpoint 锁冲突）。
  - `models/agent_runtime.py`（新）：5 表 AgentSessionDB/AgentStepDB/EpisodeDB/AutonomyAlertDB/AutonomyScanDB，挂 `Base.metadata`（`main.py` 导入以 `create_all` 建表）。
  - `session.py`/`memory.py`/`autonomy.py` 三 store 改双轨：`_cache` 快路径（SSE 实时推流依赖原地修改）+ SQLite 持久化；`get/list/delete/clear` 经 DB，`persist/record/add_alert` 写库。
  - `loop.py` `start/approve/send_message/redirect_run` 经 `_done()` 调 `persist(session)`（try/except 不阻断）；`api/v1/agent.py` 的 `abort`/`redirect`/异常分支补 persist。
  - **验收**：重启后端，会话/记忆/告警全部可读回（消除「重启即失」）。
- **A2 真实渠道 Connector**：`connectors/tiktok.py`（新）`TikTokConnector(BaseConnector)` 完整实现（真实 API 路径预留，缺 token 自动 Mock）；`__init__.py` 注册 `tiktok→TikTokConnector`。Meta/Google 抽象路径同就绪；Meta 解封只需 `config.agent_default_platform="meta"` 零改动。
- **A3 真实归因接地**：`connectors/base.py` 新增 `current_summary()`（聚合 FactMediaDaily 最新一天，roi 取 FactMMPDaily.roi_d7，无则 None）/ `account_status()`（默认 ok）/ `simulate_impact()`（全 0 ImpactEstimation 7 维）。MockMediaConnector 用 SimulationEngine 覆盖提供真实因果。`roi is None` 时检测器/规则引擎跳过（防无 MMP 误报 ROI 跌破）；`db=None` 安全返回 []。
- **验证**：`scripts/smoke_phaseA.py`（隔离 /tmp DB）13 项断言全 PASS（A1 重启可读回 / A2 tiktok 注册 / A3 Meta+TikTok execute_pull+current_summary 非空+roi=None 安全+account_status/simulate_impact 不崩 + db=None 安全）。
- **版本与文档**：`main.py` 1.7.0 → **1.8.0**；`docs/CHANGELOG.md` 新增 v1.8.0 段（路线图 Phase A 标完成、v2.0「落库」项划掉）；`docs/AGENT_ITERATION_ROADMAP.md` 状态改「执行中」、Phase A 三行标 ✅、状态核对表更新。
- **待续（自然下一步）**：Phase B 能力供给（ToolCatalog/MCP 桥/CLI 沙箱/API 自动生成 + 读工具 + 跨渠道预算 + 粒度下钻）；Phase C 复盘+知识库+市场差距（Reflector 因果归因/StrategyStore 扩维/知识库/kb_query/归因模型/日报推送）；真实 Meta 接通等账号解封、真实 MMP 拉取待密钥。

## Google Ads 真实连接器升级（真实 SDK + Mock 回退，09:11 接续）
- 用户指令："google ads 账户和计划目前是就绪的，真正的广告链路可以从google开启。" 经 AskUserQuestion 确认：**读写全开**（真实 mutation 在 L1/L2 人在环下允许）/ **用户现场提供凭证** / **现在切默认平台到 google**。
- **核心约束（沙箱硬限制）**：`pip install google-ads` / `pip install grpcio` 在本沙箱**必 OOM（exit 137）**——grpcio 大 wheel 解压触顶 RAM 上限（3.13/3.14 venv 均死，/tmp 是真盘 52G 非 tmpfs）。故**无法在沙箱内装 SDK / 无法冒烟真实链路**；设计成"真实代码 + 缺 SDK 自动回退 Mock"，在能装 google-ads 的环境（笔记本/生产/CI）填凭证即真实激活。
- 改动（接续前序未收尾的 #59-#63）：
  - `config.py`：新增 6 个 `google_*` 凭证字段（developer_token/client_id/secret/refresh_token/customer_id/login_customer_id）；`agent_default_platform` 改 `"google"`；新增 `@property google_credentials_dict`（聚 6 字段→连接器期望 key：去 `google_` 前缀，仅非 None）。
  - `connectors/google.py`：重写 `GoogleAdsConnector`——`__init__` 读凭证、`_is_mock = 凭证不全 OR SDK 不可用`（新增 `_sdk_available()` 静态方法软探查 `import google.ads.googleads`，缺失→False→回退 Mock，防真实路径入口 ImportError 崩）；`_real_client()` 懒加载 `GoogleAdsClient.load_from_dict`；`auth()/pull()/_real_pull()/_mock_pull()` 真实+Mock 双路径；`normalize()` 用 `metrics.installs` 或回退 `conversions` 近似 app installs，CPI=spend/installs；写动作 `update_campaign_status/budget`、`update_adset_bid`、`rotate_creative` 均有真实 GAQL mutate + Mock 回退；补 `import logging`（此前用了 `logger` 未 import，会 NameError——已修）。
  - `connectors/__init__.py`：新增 `resolve_credentials(platform, db, app_id)`（优先库表 `connector_credentials` active+verified，回退 `settings.google_credentials_dict`，均无→`{}`→Mock）。
  - `api/v1/agent.py` `_make_ctx` 与 `agent_runtime/autonomy.py` `scan`：原传 `credentials={}`（断线）→ 改为 `resolve_credentials(agent_default_platform, db, app_id)`。
  - `connector_service.py` `_get_default_credentials`：库表无凭证且 platform=google 时回退 `settings.google_credentials_dict`（真实数据拉取/写操作的凭证来源）。
  - `scripts/demo_phase4.py`：构造 connector 一并改用 `resolve_credentials`（一致性）。
  - `backend/.env.example`：增补 Google Ads 凭证注释段 + `AGENT_DEFAULT_PLATFORM` 说明。
- **验证**（system python3.14 + 项目依赖，27 项全 PASS）：7 文件 py_compile 通过；`google_credentials_dict` 沙箱空 `{}`；`agent_default_platform=='google'`；`resolve_credentials(db=None)→{}`；空凭证构造 `GoogleAdsConnector` → `_is_mock=True`、`auth()→True`、`pull()` mode=mock（18 行）、`normalize()` 算 cpi、`update_*` 四个写动作 Mock 成功；**关键**：塞假凭证但 SDK 缺失 → 仍 `_is_mock=True`、`pull()`/`update_*` 走 Mock 不崩；6 个关键模块 `import` 全过。
- **待用户**：把真实 Google Ads 凭证（developer_token + OAuth client_id/secret + refresh_token + customer_id，可含 MCC login_customer_id）发来。落库路径二选一：①填进 `backend/.env` 的 `GOOGLE_*` 字段（config 回退，最简单）；②或插入 `connector_credentials` 表（active+verified）走 DB 优先路径。填好后**在能装 google-ads 的机器上** `pip install google-ads` 即自动切真实链路（本沙箱仍只能 Mock）。凭证通过 `resolve_credentials` 自动注入 Agent 运行时 / 数据管道，无需改代码。
