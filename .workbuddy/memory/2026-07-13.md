# 2026-07-13

## 收到 Google OAuth 客户端凭证并写入 .env（06:10）
- 用户提供 Google Cloud OAuth 客户端 JSON（`Downloads/client_secret_707476956736-...json`，Desktop app 类型），含 `client_id` 与 `client_secret`。
- 已写入 `backend/.env`：`GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`（真实值）；`GOOGLE_DEVELOPER_TOKEN` / `GOOGLE_REFRESH_TOKEN` / `GOOGLE_CUSTOMER_ID` / `GOOGLE_LOGIN_CUSTOMER_ID` 仍为注释占位待补。`.env` 受 gitignore 保护（密钥不入库）。
- 验证（system py3.14）：`settings.google_credentials_dict` 已含 `client_id`+`client_secret`；`agent_default_platform=='google'`。连接器因仍缺 refresh_token/developer_token/customer_id（且沙箱无 google-ads SDK）→ 保持 Mock，不崩。
- 新增 `backend/scripts/get_google_refresh_token.py`：用该客户端 JSON 跑本地 OAuth（`Flow.run_local_server(port=8080)` 自动捕获回调）打印 `refresh_token`，填入 `GOOGLE_REFRESH_TOKEN` 即可（需 `pip install google-auth-oauthlib`）。
- **待用户补全**：① Google Ads 后台 API Center 的 `developer_token`（Test→Basic）；② 用脚本或 OAuth Playground 换 `refresh_token`；③ 运营账户 `customer_id`（无连字符，MCC 则再加 login_customer_id）。三者齐 + 在装了 google-ads 的机器上 `pip install google-ads` → 自动切真实 API（无需改代码）。

## 用户补交 developer_token（06:29）
- `GOOGLE_DEVELOPER_TOKEN` 已写入 `backend/.env`（真实值，不记明文于日志）。现 `google_credentials_dict` 已含 client_id+client_secret+developer_token 三项；仍缺 refresh_token、customer_id。
- 验证：`settings.google_credentials_dict.keys() == ['developer_token','client_id','client_secret']`；默认平台 google。连接器仍因缺 refresh_token/customer_id+沙箱无 SDK → Mock。
- 剩余 2 项：② 用 `get_google_refresh_token.py`（需 `pip install google-auth-oauthlib`）换 refresh_token；③ 后台左上账户 ID 去连字符填 customer_id。

## 用户要求「你自己执行下」换 refresh_token（06:33）——结论：沙箱无法代跑
- 实测沙箱代理对 Google 返回 `502 Bad Gateway`（`HTTP_PROXY=http://127.0.0.1:56301`，端口随代理软件重启变化；urllib 默认不读 env 代理变量，须显式 ProxyHandler）。即沙箱出网到 Google 被挡，连令牌端点都到不了。
- 更根本：OAuth 授权末步必须由用户本人浏览器登录 Google 点「允许」（Google 安全强制），脚本/agent 均无法代点。**此步只能用户在本机完成**。
- 已增强 `backend/scripts/get_google_refresh_token.py`：保留 `run_local_server` 全自动模式，新增 `--manual` 兜底（打印授权 URL→用户浏览器授权→复制地址栏 `?code=`→粘回终端自动换 token）。`py_compile` 通过。
- 交接给用户本机命令：`cd backend && pip install google-auth-oauthlib && python scripts/get_google_refresh_token.py /Users/hezan/Documents/downloads/client_secret_707476956736-im09klkub8jgi9u87ep725dnjdq6rq49.apps.googleusercontent.com.json` → 打印 refresh_token 发回即可。
- ⚠️ developer_token 当前为 **Test 访问级别**（API 中心截图确认），仅能访问测试账户；要调生产账户 `2046956901` 须在 API 中心申请升 **Basic access**。

## refresh_token 已拿到并写入 .env（06:42）——5/5 凭证齐备
- 用户本机跑 `get_google_refresh_token.py` 时先遇 403 org_internal（OAuth 客户端为 Internal 仅组织内）→ 指导改为 External（建议发布 Production 避免 7 天过期）→ 成功换出 refresh_token。
- `GOOGLE_REFRESH_TOKEN` 已写入 `backend/.env`。全字段校验：`client_id`+`client_secret`+`developer_token`+`refresh_token`+`customer_id` 5 项 SET，`_required_for_real_path=True`，`agent_default_platform=google`。
- 还需 2 个前置才能真正跑通生产链路：①本机装 `google-ads`（`pip install google-ads`，沙箱因 grpcio OOM 装不了）；②developer_token 从 Test 升 Basic access（截图确认当前 Test）。

## 生成 Google Ads API Basic access 设计文档（06:58）
- 用户填写 Google Ads API Basic access 申请表（Q5-Q9），需上传设计文档（.pdf/.doc/.rtf）。
- 用 docx 技能生成 `docs/google_ads_api_design_document.docx`（docx-js，含 9 节：Overview/Use&Audience/Capabilities/API Usage 含操作表/Architecture/Data Security/Permissions HITL/RateLimiting/Compliance，zip 完整性校验通过 22 entries）。
- 本机无 LibreOffice 无法转 PDF，改用 fpdf2 直接生成 `docs/google_ads_api_design_document.pdf`（注意：fpdf2 核心字体仅 Latin-1，em-dash「—」与 bullet「•」需替换为 ASCII；multi_cell 宽度须显式设 `w-r_margin-l_margin` 并在每段前 `set_x(l_margin)`，否则报 not enough horizontal space）。
- 两份文件均已产出，供用户上传 Q8。同时给出 Q7 英文描述与 Q9 选 Internal users。
- 现状：SmartUA 代码侧已 100% ready，凭证侧 5/5 齐。仅剩「沙箱无 google-ads SDK + 本机装 SDK + developer_token 升 Basic」三件事在用户侧/真实环境完成。
- **凭证安全提醒**：该 OAuth 客户端为 Desktop app，`refresh_token` 长效前提是 Google Cloud「OAuth 同意屏幕」发布状态=「生产」（否则 7 天过期）。用户提供的 JSON 仍留在 Downloads，未移动/删除。

## SDK 已装 + 真实链路代码侧验证（07:03）
- 用户确认本机已 `pip install google-ads`。实测沙箱 system Python 3.14 现已装 `google-ads 31.1.0 + grpcio 1.82.1`（site-packages 确认）——之前 OOM(137) 是临时，旧「沙箱装不了 grpcio」结论已过时，已更新 ~/.workbuddy/MEMORY.md。
- **沙箱侧「代码就绪」验证全部通过（不触网）**：①`resolve_credentials('google',db=None,app_id=1)` 返回 5 项凭证齐全；②`ConnectorFactory.get_connector('google',credentials=creds)` → `GoogleAdsConnector`，`_is_mock=False`（真实路径自动选定）；③`_sdk_available=True`；④空凭证时 `_is_mock=True`（Mock 优雅降级仍有效）；⑤`agent_default_platform='google'`。=> 代码侧真实链路 100% ready。
- **真实 API 往返仍无法在沙箱完成**：实测代理对 `accounts.google.com`(OAuth 令牌端点) 与 `googleads.googleapis.com`(gRPC host) 均 HTTP 000 超时，而 `api.github.com` 正常 200。gRPC 只认小写 `http_proxy/https_proxy`；OAuth 走 requests 读 `https_proxy`。`GoogleAdsClient.load_from_dict` 因刷新 token 失败抛 `TransportError(/o/oauth2/token 502)`。=> 沙箱出网策略挡 Google，须本机（能连 Google）验证真实 pull。
- 新增 `backend/scripts/verify_google_live.py`：端到端验证（auth 构建 + pull 近 7 天 + 可选 `--apply` 真实写预算+5%），带错误诊断（区分 GoogleAdsException 权限不足 vs TransportError 网络/代理 vs 其他；权限不足返回 2、网络错误返回 3）。`py_compile` 通过，非网络逻辑验证 OK。
- **用户侧剩余 1 件事**：developer_token 从 Test 升 **Basic access**（API 中心申请，审核几天）。升过后在本机 `cd backend && python3 scripts/verify_google_live.py` 即可看到真实生产数据。

## verify_google_live.py 修复（07:09）
- 用户本机首跑报错: `pull() missing 2 required positional arguments: 'date_from' and 'date_to'` 且 `report_api_error` 把 `TypeError` 误判成 GoogleAdsException(crash on `e.failure`)。
- 根因: 真实 `pull(self, date_from, date_to, report_type="campaign_daily", **kwargs)` 返回 `Dict{"raw_rows":[...],"metadata":{...}}`(非 list); 且 `report_api_error` 里 `from google.ads.googleads.exceptions import GoogleAdsException` 失败时回退成 `Exception`, 导致任何异常都进 GoogleAdsException 分支。
- 已重写脚本: ①`pull(date_from, date_to)` 计算近 N 天; ②按 dict 解析(`res["raw_rows"]` / `metadata["mode"]`, cost_micros/1e6=spend); ③`report_api_error` 的 GoogleAdsException import 失败时置 `None` 且加 `hasattr(e,"failure")` 守卫; ④`--apply` 改为首个 campaign `PAUSED->ENABLED` 往返(可逆、零副作用)。`py_compile` 通过, 离线用 `_mock_pull` 验证 dict 解析逻辑正确。
- 用户重跑命令不变: 本机 `cd backend && python3 scripts/verify_google_live.py` (加 `--apply` 顺带验写动作)。

## 账户结构澄清：2046956901 是 MCC 经理账户，无 Campaign（10:01）
- 用户发账户下拉截图：列表含 `zhanbo growth lab 758-624-2509`(客户账户，有 Campaign #1)、`zhanbogrowthlab 204-695-6901 经理`(MCC)、`zhanbogrowthlab 971-946-3864 经理`(MCC)。
- **关键发现**：当前 `backend/.env` 的 `GOOGLE_CUSTOMER_ID=2046956901` 填的是 **MCC 经理账户**——经理账户本身没有广告系列，API 拉不到 campaign。真正有 campaign 的是客户账户 **758-624-2509**。
- 因此选项：①直接以客户账户调用——`GOOGLE_CUSTOMER_ID=7586242509`，`login_customer_id` 留空，但 refresh_token 须是该客户账户登录换的（当前 refresh_token 是在 MCC 下换的，不匹配）；②挂 MCC 统一调用（推荐）——把 758-624-2509 关联到 MCC 204-695-6901（MCC 侧「子账号设置→关联现有账号」+ 客户账户侧「访问权限→经理」接受），然后 `.env` 设 `GOOGLE_CUSTOMER_ID=7586242509` + `GOOGLE_LOGIN_CUSTOMER_ID=2046956901`，复用现有 MCC 的 refresh_token。
- 叠加约束仍成立：①developer_token 仍为 **Test 级别**（仅测试账户可访问，生产账户 758... 需 Basic access 审批通过）；②沙箱代理对 Google 全部超时，真实 pull 只能用户本机跑。
- **结论**：即便配置改对，只要 developer_token 还是 Test + 账户是生产，本机 `verify_google_live.py` 仍会报权限不足；须等 Basic 审批 + 正确 customer_id 才能真拉到 campaign。代码侧已 ready。

## 子账户已关联到 MCC + .env 修正（10:08）
- 用户确认子账户（758-624-2509，有 Campaign #1）已关联到 MCC（204-695-6901）。
- 已改 `backend/.env`：`GOOGLE_CUSTOMER_ID=7586242509`（原错填的 MCC 2046956901 改为真实客户账户）、`GOOGLE_LOGIN_CUSTOMER_ID=2046956901`（MCC，复用其 refresh_token）。
- 验证（system py3.14）：`google_credentials_dict` 现含 `customer_id=7586242509` + `login_customer_id=2046956901` + 其余 4 项；`ConnectorFactory.get_connector('google',credentials=...)` → `GoogleAdsConnector` 且 `_is_mock=False`（真实路径选定）。
- **剩余唯一阻塞**：developer_token 仍为 **Test 级别**，生产子账户 758... 仍不可被 API 访问；须等 Basic access 审批通过。审批后本机 `python3 scripts/verify_google_live.py` 即可拉到真实 campaign。沙箱侧仍因代理挡 Google 无法真跑。
