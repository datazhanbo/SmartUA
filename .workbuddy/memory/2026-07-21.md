# 2026-07-21

## Google Ads 验证阻塞点澄清（用户问询）
- 关键区分：**开发者令牌访问级别（Test）≠ 账户类型**。当前 `GOOGLE_DEVELOPER_TOKEN` 仍为 Test 级别，只能调 test account 的 API，**无法访问生产子账户 7586242509**（挂在 MCC 2046956901 下，真实 campaign）。
- 结论：Test 令牌下本机跑连接器会选中真实路径（`_is_mock=False`）但被 Google 鉴权拒绝（`AuthorizationError`，生产账户不可达）。无法拉真实 campaign，只能等 Basic access 批准。
- 两条验证路径：A) 在测试环境建 test account 配 `GOOGLE_CUSTOMER_ID` 跑通全链路（数据合成但代码被真实 API 验证）；B) 等 Basic 批下来直接拉 7586242509。
- 沙箱网络约束仍在：即便 Basic 批下，沙箱代理对 `googleads.googleapis.com` 返回 000/502，真实调用只能在本机 `scripts/verify_google_live.py` 跑。
- 待用户决定：是否准备 test account 验证清单 / 增强 verify 脚本错误提示 / 维持等 Basic。

## 更正：Google 测试账户必须独立层级（非挂生产 MCC）
- 经核对官方文档（developers.google.cn/google-ads/api/docs/best-practices/test-accounts）：**测试账号与生产账号完全不能互动，无法挂在现有生产 MCC 2046956901 下**。必须新建独立「测试经理账号」作根。
- 正确接入方式（已给用户操作指引）：
  - 进 `https://ads.google.com/aw/testaccounts` 用 zanhe1984@gmail.com 登录；
  - 建 test manager account（ID 以 9 开头）→ 记此 ID 作 `GOOGLE_LOGIN_CUSTOMER_ID`；
  - 在其下建 test client account（ID 以 9 开头）→ 记此 ID 作 `GOOGLE_CUSTOMER_ID`；
  - `GOOGLE_DEVELOPER_TOKEN` 用生产经理账号的 token（R2YSL...，Test 令牌即可调 test account）；refresh_token 复用现有（同 Google 身份）。
  - ⚠️ 之前回给用户"保持 GOOGLE_LOGIN_CUSTOMER_ID=2046956901"是错的，已更正为测试经理 ID。
- 限制：测试账号 serving 指标全空（报表可能全 0，正常）；不能测出价模拟/转化上传/结算；层级上限 50 账号；连续 1 年无活动自动删除。
- 沙箱仍不能直连 Google，验证须在本机 `scripts/verify_google_live.py`。
