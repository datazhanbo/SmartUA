# 2026-07-12

## 阅读 GPT-5.6 的 SmartUA upgrade 建议 + 产出统一迭代路线图
- 用户引用外部建议文档 `/Users/hezan/Documents/hezan/project/smartua/tabbit   gpt5.6 sol 给的upgrade建议.md`（GPT-5.6 给的升级建议），要求阅读后给"进一步迭代计划"，确认后执行。
- 文档核心判断：**最大瓶颈是"真实数据土壤"缺失**（无真实渠道/归因，agent 在沙盘打仗）；建议分 🔴必须(持久化/真实Connector/MMP归因) 🟡中期(跨渠道预算/MTA/粒度下钻/策略A-B/日报推送) 🟢长期(RL/多Agent/A2A/复杂目标分解)。
- **代码实证核对**，修正文档过时处（基线 v1.6，现 v1.7）：
  - 会话/记忆仍内存：`session.py:105 self._sessions: Dict= {}` → 持久化确为真实缺口。
  - 策略已持久：`strategy.py:157 json.dump` 到 `agent_strategy_path` → 策略持久已半解决。
  - LLM 已真实接入 Ark（非规则降级）；market_research 外部检索已具备。
  - 仅 Mock connector；粒度仅 Campaign；归因仅 mock。
- **产出统一路线图** `docs/AGENT_ITERATION_ROADMAP.md`：合并用户上轮三支柱(工具/复盘/知识库) + GPT 建议，分 Phase A(数据地基)→B(能力供给)→C(复盘+知识+市场差距)→D(领先市场)。核心论点：地基→智能→领先递进，无 A 则 B/C 瞎、无 B/C 则 A 不会用。
- 首迭代建议：A1 持久化(无外部依赖,立刻做) + B1 ToolCatalog 骨架 + C4 知识库表结构；A2/A3 写代码+Mock 待命(受 Meta 封/缺密钥制约)。
- 已用 AskUserQuestion 向用户确认 5 个决策点（首迭代范围/真实渠道策略/向量检索/人审强度/Phase D 是否纳入），待回复后执行。
